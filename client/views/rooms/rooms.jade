h4 Rooms Finder &amp; Creator

.row
  .col-xs-3
    form(role='form', novalidate, name='roomsForm')
      .form-group
        label(for='name') Name
        input.form-control#name(type='text', name='name', ng-model='room.name', autofocus)
      .form-group
        label(for='password') Password
        input.form-control#password(type='password', name='password', ng-model='room.password')
      .form-group
        button.btn.btn-primary(ng-click='submit()') Create Room
  .col-xs-9
